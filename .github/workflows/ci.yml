# name: End-to-end tests 🧪
# on: [push]
# jobs:
#   cypress-run:
#     runs-on: ubuntu-20.04
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v3
#       # Install NPM dependencies, cache them correctly
#       # and run all Cypress tests
#       - name: Cypress run
#         uses: cypress-io/github-action@v6
#         with:
#           config-file: cypress.config.js

name: End-to-end tests 🧪

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  cypress-run:
    runs-on: ubuntu-20.04

    services:
      my-service:
        image: my-service-image # Substitua pela imagem do seu serviço se estiver usando Docker
        ports:
          - 3000:3000 # Certifique-se de que esta porta é a mesma que a usada pelo seu serviço
        options: --health-cmd="curl --silent --fail http://localhost:3000/health" --health-interval=10s

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install dependencies
        run: npm ci

      - name: Start server
        run: |
          npm start &
          sleep 10

      - name: Run Cypress tests
        uses: cypress-io/github-action@v6
        with:
          config-file: cypress.config.js
